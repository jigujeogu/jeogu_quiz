# 스탠바이미 1세대 성능 개선 작업 지시서

## 1. 이 문서의 목적

이 프로젝트는 1080×1920 세로 화면용 정적 HTML 퀴즈다. LG 스탠바이미 1세대에서 실행하면 다음 문제가 보인다.

- 서로 다른 HTML 페이지로 전환할 때 전체적으로 느리다.
- 타이핑 애니메이션이 버벅일 때가 있다.
- `도로화면/scene1.html`, `도로화면/scene2.html`에서 저구가 윙크하며 확대될 때 끊긴다.
- 글자와 그래픽이 동시에 나타나야 하는데 그래픽이 조금 늦게 보일 때가 있다.

아래 지시를 위에서부터 순서대로 수행한다. 한꺼번에 구조를 전면 재작성하지 않는다.

## 2. 절대 지켜야 할 조건

1. 화면 디자인, 문구, 색상, 크기, 위치, 애니메이션 순서와 사용자 진행 흐름을 임의로 변경하지 않는다.
2. `index.html`의 iframe 및 `postMessage` 기반 화면 전환 구조는 유지한다.
3. 파일·폴더 이름을 바꾸지 않는다. 한글, 공백, 괄호가 포함된 현재 경로를 그대로 사용한다.
4. 정답 판정, 진행 상태, 뒤로가기, 20초 미입력 복귀, BGM 및 효과음 동작을 보존한다.
5. 성능 개선과 관계없는 리팩터링이나 코드 포맷 전체 변경을 하지 않는다.
6. 각 단계가 끝날 때 반드시 전체 진행 흐름을 다시 확인한다.
7. 이미지가 준비되지 않은 상태에서 단순히 지연 시간을 늘리는 방식으로 문제를 숨기지 않는다. 이미지의 실제 `load`와 `decode()` 완료 여부를 사용한다.
8. 수정 전에 `git status --short`를 확인한다. 기존 사용자 변경 사항이 있으면 덮어쓰지 않는다.

## 3. 프로젝트 진행 흐름

```text
index.html
  → 도로화면/scene1.html
  → 퀴즈1 전반부/index.html
  → 퀴즈1/quiz_game_(4-7).html
  → 도로화면/scene2.html
  → 퀴즈2(0824)/integrated.html
  → 도로화면/scene3.html
  → 처음으로 복귀
```

공통 파일의 역할:

- `index.html`: iframe 교체, 전체 BGM, 전체 화면 처리
- `bgm.js`: iframe의 사용자 상호작용을 부모에 전달
- `back_button.js`: 뒤로가기 UI, 팝업, 20초 미입력 복귀
- `quiz_sfx_b64.js`: 퀴즈 효과음 데이터
- `도로화면/sounds/siren_b64.js`: 도로화면 사이렌 데이터

## 4. 이미 수행된 QA 결과

Chrome의 1080×1920 환경에서 HTTP 서버로 실행해 측정했다. 절대 시간은 실제 서버와 스탠바이미에서 달라질 수 있지만, 페이지 간 상대적인 차이와 병목 위치는 참고할 수 있다.

### 최초 페이지 로드

| 페이지 | 로드 시간 | 요청 리소스 수 |
|---|---:|---:|
| 도로화면 scene1 | 342ms | 23개 |
| 퀴즈1 전반부 | 522ms | 20개 |
| 퀴즈1 본 게임 | 1,470ms | 44개 |
| 도로화면 scene2 | 346ms | 24개 |
| 퀴즈2 | 699ms | 27개 |
| 도로화면 scene3 | 282ms | 21개 |

### 프레임 측정

GPU가 활성화된 일반 데스크톱 환경:

- scene1: 약 59fps, 최대 프레임 간격 50ms
- scene2: 약 47fps, 최대 프레임 간격 416.6ms
- 퀴즈2: 약 60fps

저사양 스트레스 환경에서 타이핑과 저구 확대를 동시에 실행한 결과:

- scene1: 약 12.8fps, 최대 프레임 간격 233ms
- scene2: 약 14.2fps, 최대 프레임 간격 134ms

### 메모리 누수 검사

여섯 페이지를 5회씩, 총 30회 iframe 전환한 후 매 회 강제 GC했다.

- JS 힙: 1.117MB → 1.116MB
- 이벤트 리스너: 108개로 유지
- DOM 노드: 777 → 780 이후 유지
- 오디오 핸들러: 2개로 유지

현재 확인 범위에서는 반복 전환으로 계속 증가하는 명확한 메모리 또는 AudioContext 누수는 없다. 현재의 핵심 문제는 누수보다 페이지 진입 시 작업 집중과 비효율적인 렌더링이다.

## 5. 수정 우선순위

반드시 `A → B → C → D → E → F` 순서로 진행한다. 각 단계별 테스트에 실패하면 다음 단계로 넘어가지 않는다.

---

## A. scene2의 레이아웃 애니메이션을 transform 기반으로 변경

### 수정 대상

- `도로화면/scene2.html`
- 대략 CSS 325~400행의 다음 애니메이션:
  - `dropAnimation`
  - `popAndDropLeft`
  - `popAndDropRight`

### 현재 문제

무한 애니메이션 중 `top`, `left`, `right` 값을 계속 바꾸고 있다. 이 속성은 프레임마다 레이아웃과 페인트를 다시 일으킬 수 있다. scene2에서 측정된 416.6ms 프레임 지연의 가장 유력한 원인이다.

### 수정 방법

1. 움직이는 요소의 기준 위치는 고정된 `top`, `left` 또는 `right`로 한 번만 지정한다.
2. 키프레임 안에서는 위치 이동을 `transform: translate3d(x, y, 0)`으로 표현한다.
3. 회전이 필요하면 같은 `transform` 안에 `rotate(...)`를 함께 넣는다.
4. 키프레임 안에서 `top`, `left`, `right`, `width`, `height`를 애니메이션하지 않는다.
5. 기존 시작 위치, 낙하 경로, 회전 방향, 등장·퇴장 시점은 시각적으로 동일하게 유지한다.
6. `will-change`에는 실제로 애니메이션하는 `transform`, `opacity`만 남긴다. `top`, `left`, `right`는 제거한다.

### 완료 조건

- scene2에서 움직이는 그래픽의 경로가 기존과 육안상 동일하다.
- `@keyframes` 내부에 움직임을 위한 `top`, `left`, `right` 변경이 남아 있지 않다.
- scene2 진입, 타이핑, 경고 화면, 저구 확대, 다음 페이지 이동이 정상이다.

---

## B. scene1·2의 저구 확대 이미지를 실제로 준비한 뒤 애니메이션 시작

### 수정 대상

- `도로화면/scene1.html`
- `도로화면/scene2.html`
- 필요하면 같은 패턴을 `도로화면/scene3.html`에도 적용

### 현재 문제

페이지를 표시하기 전에 배경 이미지 한 장만 기다린다. 저구 확대에 사용하는 `big-jeogu-1.png`, `big-jeogu-2.png`와 다음 화면 그래픽은 준비 여부를 확인하지 않는다. scene1의 확대 이미지는 `decoding="async"`이므로 표시 시점까지 디코딩이 미뤄질 수 있다.

페이지 `load` 이후 모든 이미지 디코딩이 끝날 때까지 scene1·2에서 데스크톱 기준 약 80ms가 추가로 필요했다.

### 수정 방법

1. 공통 형태의 이미지 준비 함수를 각 파일 안에 만든다.

```js
function prepareImage(img) {
  if (!img) return Promise.resolve();

  const loaded = img.complete && img.naturalWidth > 0
    ? Promise.resolve()
    : new Promise(resolve => {
        img.addEventListener('load', resolve, { once: true });
        img.addEventListener('error', resolve, { once: true });
      });

  return loaded.then(() => {
    if (typeof img.decode === 'function') {
      return img.decode().catch(() => {});
    }
  });
}
```

2. DOM을 읽은 직후 아래 이미지들의 준비 Promise를 미리 시작한다.
   - `bigJeogu1`
   - `bigJeogu2`
   - 전환 직후 바로 표시하는 퀴즈 화면의 핵심 이미지
3. 사용자가 마지막 문구를 넘겼을 때 확대 애니메이션을 바로 실행하지 말고, 위 Promise가 끝난 뒤 기존 확대 시퀀스를 시작한다.
4. 준비 중 중복 터치를 막는 Boolean 잠금을 둔다.
5. 실패한 이미지 때문에 영원히 멈추지 않도록 `error`도 완료로 처리한다. 필요하면 2초 이내의 안전 타임아웃을 추가한다.
6. 단순한 고정 `setTimeout` 증가는 하지 않는다.

### 완료 조건

- 저구의 첫 확대 프레임부터 이미지가 보인다.
- 윙크 이미지 전환 시 빈 프레임이나 깜빡임이 없다.
- 빠르게 연속 터치해도 확대 시퀀스가 두 번 시작되지 않는다.

---

## C. 도로화면 타이핑에서 매 글자마다 전체 innerHTML을 교체하지 않도록 수정

### 수정 대상

- `도로화면/scene1.html`의 `playTypingEffect`
- `도로화면/scene2.html`의 `playTypingEffect`
- `도로화면/scene3.html`의 같은 함수

### 현재 문제

현재 코드는 약 50ms마다 누적된 문자열 전체와 커서를 다시 `innerHTML`에 넣는다. 이 때문에 HTML 재파싱, DOM 삭제·생성, 스타일 계산과 임시 문자열 생성이 매 글자마다 발생한다.

문장 안에는 `<br>`과 scene2의 `<span style="...">`가 있으므로 태그를 단순 제거하면 안 된다.

### 권장 구현

1. `htmlString`을 `<template>`에 한 번만 파싱한다.
2. 파싱된 DOM 구조와 `<br>`, `<span>`은 그대로 유지한다.
3. `TreeWalker`로 텍스트 노드만 순서대로 수집한다.
4. 각 텍스트 노드의 원문을 별도 배열에 저장한 뒤 화면의 텍스트 노드는 빈 문자열로 만든다.
5. 완성된 DOM 구조를 `element`에 한 번만 삽입한다.
6. 커서 `<span class="blinking-cursor">`도 한 번만 만들고 마지막에 추가한다.
7. 타이핑 틱마다 현재 텍스트 노드의 `nodeValue`만 변경한다.
8. 기존 한글 초성·중성·종성 타이핑 표현은 유지한다. 기존 `getTypingSteps()`를 텍스트 노드의 순수 텍스트 단위로 재사용할 수 있다.
9. 사용자가 타이핑 중 화면을 눌러 전체 문장을 즉시 표시하는 기능은 유지한다. 스킵 시 한 번 `innerHTML = currentFullHTML`을 사용하는 것은 허용된다.
10. 새 문구 시작 및 페이지 종료 시 기존 타이머를 반드시 취소한다.

### 금지 사항

- 매 틱마다 `element.innerHTML = ...` 실행
- 매 틱마다 커서 DOM 새로 생성
- 문장의 모든 누적 prefix 문자열을 미리 배열에 저장

### 완료 조건

- 한글 조합 과정과 타이핑 속도가 기존과 같다.
- 줄바꿈과 강조 배경 스타일이 보존된다.
- 타이핑 중 터치 스킵이 정상이다.
- 문구를 여러 번 넘겨도 이전 타이머가 남아 글자가 섞이지 않는다.

---

## D. 퀴즈1의 첫 진입 리소스 집중 완화

### 수정 대상

- `퀴즈1/quiz_game_(4-7).html`
- 대략 732~904행의 화면 4~7 이미지
- 대략 920~954행의 `allGraphicAssets` 전체 사전 로딩

### 현재 문제

숨겨진 화면 5~7의 `<img src="...">`도 HTML 파싱 즉시 로드된다. 동시에 `allGraphicAssets`가 모든 이미지에 대해 다시 `new Image()`를 만든다. 브라우저 캐시가 중복 다운로드를 막더라도 첫 진입에서 44개 리소스의 네트워크·SVG 파싱·이미지 디코딩이 몰린다.

### 수정 방법

1. 항상 필요한 공통 배경과 최초 화면 4의 이미지는 정상 `src`를 유지한다.
2. 숨겨진 화면 5~7에서만 필요한 이미지는 `src` 대신 `data-src`에 경로를 둔다.
3. `prepareScreen(screenId)` 함수를 만든다.
   - 해당 화면의 `img[data-src]`를 찾는다.
   - `img.src = img.dataset.src`로 실제 로드를 시작한다.
   - `load`와 `decode()` 완료 Promise를 반환한다.
   - 두 번 호출해도 다시 작업하지 않도록 캐시한다.
4. 초기 화면 4를 표시한 뒤 유휴 시간에 화면 5만 준비한다.
5. 화면 5로 이동한 뒤 화면 6을 준비하고, 화면 6 진입 뒤 화면 7을 준비한다.
6. 다음 화면 준비가 끝나지 않았다면 화면 교체 직전에 Promise를 기다린다. 이때 정답 버튼 중복 입력을 잠근다.
7. `allGraphicAssets` 전체 일괄 preload는 제거하거나 공통 자산과 현재/다음 화면 자산만 처리하도록 축소한다.
8. 정답·오답 팝업은 첫 문제 진행 중 유휴 시간에 미리 준비한다.
9. `requestIdleCallback`을 쓸 경우 webOS 호환을 위해 `setTimeout` fallback을 반드시 둔다.

```js
const runWhenIdle = window.requestIdleCallback
  ? cb => requestIdleCallback(cb, { timeout: 1000 })
  : cb => setTimeout(cb, 100);
```

### 완료 조건

- 퀴즈1 최초 진입에서 화면 6·7 전용 이미지가 즉시 요청되지 않는다.
- 화면 4→5→6→7 전환 시 빈 이미지가 보이지 않는다.
- 정답·오답 팝업과 축하 팝업이 처음부터 정상 표시된다.
- 최초 로드 리소스 수와 로드 시간이 수정 전보다 감소한다.

---

## E. 퀴즈2 팝업 이미지를 선택 전에 준비

### 수정 대상

- `퀴즈2(0824)/integrated.html`
- 대략 787행의 `popup-img`
- 대략 790행의 `popup-circle-icon`
- 대략 1064~1071행의 `quizMap`
- 대략 1157~1204행의 `showPopup`

### 현재 문제

두 팝업 이미지가 초기 HTML에서 `src=""`이고, 사용자가 답을 선택한 순간에 경로를 넣은 뒤 즉시 애니메이션을 시작한다. 로컬 데스크톱에서도 각 팝업+아이콘 쌍의 준비에 51~76ms가 걸렸다.

### 수정 방법

1. 초기 HTML의 빈 `src=""` 속성을 제거한다. `src` 속성 자체가 없는 상태로 둔다.
2. `quizMap` 생성 직후 모든 popup과 icon 경로를 `Image` 객체로 미리 요청한다.
3. 각 항목별 준비 Promise를 Map에 저장한다.
4. `showPopup(id)`는 중복 입력을 잠근 뒤 해당 항목의 `load`와 `decode()`가 끝나면 다음 순서로 실행한다.
   1. 실제 `<img>`에 `src` 지정
   2. 오버레이 표시
   3. 강제 리플로우는 꼭 필요한 경우에만 한 번 실행
   4. 팝업과 아이콘 애니메이션 시작
5. 이미지 오류 시에도 입력 잠금이 풀리도록 `finally` 처리를 한다.

### 완료 조건

- 첫 번째 정답 선택에서도 오버레이와 이미지가 같은 프레임 묶음에서 나타난다.
- 팝업 이미지가 늦게 끼어들거나 순간적으로 빈 상태가 보이지 않는다.
- 다섯 항목, 진행 바, 팝업 닫기, 축하 화면이 모두 정상이다.

---

## F. 초기 오디오 디코딩과 외부 CDN 의존성 정리

### 수정 대상

- `도로화면/scene1.html`
- `도로화면/scene2.html`
- `도로화면/scene3.html`
- `퀴즈1/quiz_game_(4-7).html`
- `퀴즈2(0824)/integrated.html`

### 오디오 수정

현재 도로화면은 페이지 진입 즉시 Base64 사이렌을 ArrayBuffer로 변환하고 디코딩하며, 전체 오디오 샘플을 앞뒤로 순회한다.

1. 페이지 파싱 도중 즉시 디코딩하지 않는다.
2. 첫 화면이 그려진 뒤 유휴 시간 또는 최초 사용자 상호작용 시 한 번만 `ensureSirenReady()`를 실행한다.
3. 같은 페이지 안에서 중복 디코딩되지 않도록 Promise를 캐시한다.
4. 사이렌이 필요한 시점까지 준비되지 않았다면 준비 Promise 후 재생한다.
5. `AudioBufferSourceNode`는 정지할 때 `stop()`과 `disconnect()`를 호출한다.
6. 기존 볼륨, 루프 시작·끝, BGM 동작은 변경하지 않는다.

### hangul-js 수정

도로화면 3개가 `https://unpkg.com/hangul-js`를 parser-blocking 방식으로 사용한다.

1. 가능하면 사용 중인 정확한 버전을 프로젝트 안의 `vendor/` 폴더에 저장한다.
2. 라이선스 파일 또는 라이선스 주석을 함께 보존한다.
3. 세 도로화면의 script 경로를 동일한 로컬 파일로 변경한다.
4. 인터넷을 끊은 상태에서도 한글 가상 키보드 입력이 되는지 확인한다.
5. 의존성 파일을 정상적으로 확보할 수 없다면 임의 구현을 만들지 말고 이 항목만 보류했다고 보고한다.

### 완료 조건

- 첫 화면 렌더링 전에 불필요한 오디오 디코딩이 실행되지 않는다.
- 사이렌과 정답 효과음은 필요한 순간 정상 재생된다.
- 인터넷 연결 없이도 도로화면의 한글 키보드가 동작한다.

---

## 6. 낮은 우선순위 개선

위 A~F가 모두 통과한 뒤에만 검토한다.

### 퀴즈2 50ms 타이머

`퀴즈2(0824)/integrated.html`의 `TIMER_TICK_MS = 50`은 초당 20회 DOM을 확인·갱신한다.

- 디자인상 1/100초 표시가 꼭 필요하지 않다면 100ms로 낮춘다.
- 페이지가 숨겨졌을 때는 타이머를 중단하고 `pageshow` 또는 `visibilitychange`에서 다시 시작한다.
- 텍스트가 바뀌지 않았으면 DOM에 쓰지 않는 현재 방어 코드는 유지한다.

### 상시 will-change

- 애니메이션하지 않는 상태에서는 `will-change`를 제거한다.
- 애니메이션 시작 직전에 class로 추가하고 `animationend` 후 제거하는 방식을 우선한다.
- 너무 많은 대형 레이어를 동시에 GPU 레이어로 승격하지 않는다.

## 7. 필수 회귀 테스트

수정 후 아래를 처음부터 끝까지 직접 실행한다.

### 전체 흐름

1. `index.html`을 HTTP 서버 URL로 연다.
2. scene1에서 시작 버튼을 누른다.
3. 한글·영문·숫자 키보드를 각각 사용해 본다.
4. 이름 입력 후 모든 문구를 진행한다.
5. 경고·사이렌·저구 확대·윙크·퀴즈1 전반부 전환을 확인한다.
6. 퀴즈1 전반부에서 본 게임으로 이동한다.
7. 퀴즈1에서 오답과 정답을 모두 한 번 이상 선택한다.
8. 화면 4~7을 완료하고 scene2로 이동한다.
9. scene2의 모든 문구, 움직이는 그래픽, 저구 확대를 확인한다.
10. 퀴즈2에서 다섯 항목을 모두 찾고 모든 팝업을 닫는다.
11. 축하 화면에서 scene3으로 이동한다.
12. scene3 완료 후 처음 화면으로 복귀한다.

### 공통 기능

- 모든 페이지에서 뒤로가기 버튼과 메인 복귀 팝업 확인
- 20초 미입력 후 첫 화면 복귀 확인
- BGM이 페이지 전환 중 끊기지 않는지 확인
- 사이렌과 정답·오답 효과음 확인
- 빠른 연속 터치 시 중복 전환·중복 팝업이 없는지 확인
- 콘솔 error 및 unhandled rejection이 없는지 확인
- 누락 이미지와 404가 없는지 확인

### 성능 확인

1. 캐시를 비우고 최초 흐름을 측정한다.
2. 캐시가 있는 상태로 전체 흐름을 두 번 더 반복한다.
3. scene1·2에서 타이핑과 그래픽이 동시에 움직일 때 눈에 띄는 멈춤이 없는지 확인한다.
4. 저구 확대 첫 프레임과 윙크 교체 시점에 빈 이미지가 없는지 확인한다.
5. 퀴즈2 첫 팝업이 오버레이보다 늦게 나타나지 않는지 확인한다.
6. 전체 흐름을 최소 5회 반복하고 JS 힙, DOM 노드, 이벤트 리스너가 매 회 계속 증가하지 않는지 확인한다.

## 8. 최종 보고 형식

작업이 끝나면 다음 형식으로 보고한다.

```text
수정한 파일:
- 경로: 변경 내용 한 줄 설명

해결한 문제:
- 문제 → 적용한 방법 → 확인 결과

성능 비교:
- 수정 전 수치
- 수정 후 수치
- 측정 환경

회귀 테스트:
- 통과 항목
- 실패 또는 실기기 확인이 필요한 항목

남은 위험:
- 스탠바이미 실기기에서 추가로 확인해야 할 내용
```

## 9. 작업 중단 후 질문해야 하는 경우

다음 상황에서는 추측해서 진행하지 말고 사용자에게 먼저 질문한다.

- 디자인이나 애니메이션 경로를 변경해야만 성능 목표를 맞출 수 있는 경우
- 효과음 재생 시점 또는 타이핑 속도를 바꿔야 하는 경우
- `hangul-js`의 정확한 버전이나 라이선스를 확인할 수 없는 경우
- 기존 사용자 변경 사항과 수정 대상 코드가 겹치는 경우
- 스탠바이미 실기기에서만 재현되고 로컬에서는 원인을 구분할 수 없는 경우

