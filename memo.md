# 스탠바이미 1 웹브라우저 실행 보완 사항

## 1. 문서 목적

이 문서는 현재 프로젝트를 LG 스탠바이미 1 27인치의 webOS 기본 웹브라우저에서 세로형 터치 콘텐츠로 운영할 때 보완이 필요하다고 판단되는 부분을 정리한 문서다.

- 대상 기기: LG 스탠바이미 1 27인치 (`27ART10AKPL` 계열)
- 기기 해상도: FHD `1920 × 1080`
- 콘텐츠 기준 해상도: 세로 `1080 × 1920`
- 예상 사용 형태: 화면을 세로로 회전한 후 기본 웹브라우저에서 실행
- 조사 기준: 현재 저장소의 정적 코드 검토

실제 webOS 브라우저의 viewport, 음원 정책, 터치 처리 성능은 코드 검토만으로 확정할 수 없으므로 실기기 검증이 별도로 필요하다.

## 2. 종합 판단

현재 프로젝트는 `1080 × 1920` 세로 화면과 터치 입력을 전제로 설계되어 있어 스탠바이미 1의 세로 사용 형태와 기본 방향은 잘 맞는다. 다만 현재 상태는 실기기 검증 전의 프로토타입에 가깝다.

운영 전에 우선 보완해야 할 핵심 항목은 다음과 같다.

1. 외부 CDN 의존성 제거
2. 누락된 효과음 파일 처리
3. webOS 세로 viewport 및 브라우저 UI 대응 확인
4. webOS 브라우저에서 오디오·터치·스크롤 호환성 확인
5. 장시간 실행 시 성능 및 메모리 안정성 확인
6. 배포 및 실행 방식을 명확히 정의

## 3. 발견 사항 및 해결 방안

### 3.1 외부 CDN에 핵심 기능이 의존함

#### 발견된 점

도로화면이 외부 서버에서 JavaScript를 불러온다.

- `도로화면/scene1.html`, `scene2.html`, `scene3.html`
  - `https://unpkg.com/hangul-js`

도로 화면의 커스텀 한글 키보드는 `window.Hangul.assemble()` 및 `window.Hangul.disassemble()`에 직접 의존한다.

Pretendard 폰트 CDN 의존성은 2026-08-28 기준으로 해결했다. 도로화면 3개와 퀴즈 1 통합본을 루트의 `fonts/PretendardVariable.woff2`로 전환했으며, 퀴즈 1 전반부와 퀴즈 2 통합본도 같은 로컬 폰트를 사용한다.

#### 예상되는 문제

- 전시장 Wi-Fi가 느리거나 끊기면 한글 키보드가 동작하지 않을 수 있다.
- CDN 접속이 차단되거나 인증서 문제가 생기면 JavaScript 로딩에 실패할 수 있다.
- CDN 캐시 여부에 따라 첫 실행과 재실행 결과가 달라질 수 있다.

#### 해결 방법

- `hangul-js` 배포 파일을 프로젝트 내부에 저장하고 상대 경로로 불러온다.
- 외부 스크립트가 없어도 게임 진행 자체는 중단되지 않도록 방어 코드를 추가한다.

예시:

```js
if (!window.Hangul) {
  // 사용자 안내 또는 기본 입력 방식으로 대체
}
```

#### 검증 방법

스탠바이미의 네트워크를 끊은 상태에서 브라우저 캐시를 지우고 처음부터 끝까지 실행한다.

---

### 3.2 사용하지 않는 효과음 참조 코드 잔존 — 해결 완료

#### 발견된 점

`도로화면/scene1.html`이 더 이상 사용하지 않는 다음 효과음 파일을 참조하고 있었다.

```text
도로화면/sounds/common/click_sound_1.mp3
도로화면/sounds/common/jeogu_touch.mp3
```

2026-08-27 기준으로 두 음원의 `Audio` 객체와 재생 이벤트 리스너를 `scene1.html`에서 제거했다. `scene2.html`과 `scene3.html`에는 해당 참조가 없음을 확인했다.

#### 기존에 예상되었던 문제

- 해당 요청에서 HTTP 404가 발생한다.
- 일반 클릭 효과음과 캐릭터 터치 효과음이 재생되지 않는다.
- 반복되는 404가 브라우저 콘솔을 오염시키고 디버깅을 어렵게 만든다.
- 오디오 재생 실패를 별도로 처리하지 않는 구간에서는 브라우저별 경고나 예외 가능성이 있다.

#### 적용한 해결 방법

- 사용하지 않는 `click_sound_1.mp3` 및 `jeogu_touch.mp3`의 `Audio` 생성 코드를 제거했다.
- 두 음원을 재생하기 위해 추가되었던 전역 클릭 및 캐릭터 터치 이벤트 리스너를 제거했다.
- 게임 진행, 대사 전환, 캐릭터 눈동자 애니메이션 등 다른 입력 로직은 유지했다.

#### 검증 방법

전체 저장소 검색에서 두 파일명과 관련 변수 참조가 남아 있지 않은지 확인하고, 도로화면 진행용 클릭·터치가 기존과 동일하게 동작하는지 확인한다.

---

### 3.3 세로 화면 회전이 실제 브라우저 viewport에 반영되는지 미확인

#### 발견된 점

콘텐츠는 `1080 × 1920`을 기준으로 하며 각 화면에서 다음과 유사한 계산으로 축소한다.

```js
const scale = Math.min(
  window.innerWidth / 1080,
  window.innerHeight / 1920
);
```

이 계산 자체는 세로 viewport에서는 적절하다. 하지만 스탠바이미를 물리적으로 세로 회전했을 때 기본 웹브라우저가 실제 viewport도 `1080 × 1920` 방향으로 제공하는지는 실기기에서 확인되지 않았다.

#### 예상되는 문제

- 브라우저가 가로 viewport를 유지하면 콘텐츠가 약 56% 크기로 축소된다.
- 화면 가운데 좁은 세로 콘텐츠만 표시되고 좌우에 큰 여백이 생길 수 있다.
- 브라우저 주소창이나 조작 바가 남아 있으면 화면이 조금 더 축소된다.
- 회전 직후 `resize` 이벤트나 viewport 값 갱신이 늦으면 위치가 순간적으로 어긋날 수 있다.

#### 해결 방법

- 실기기에서 세로 회전 전후의 `window.innerWidth`, `window.innerHeight`, `screen.width`, `screen.height`를 기록한다.
- 브라우저 전체 화면 또는 UI 최소화가 가능한 운영 절차를 정한다.
- `orientationchange`, `resize`, `visualViewport.resize` 발생 시 레이아웃을 다시 계산한다.
- 지원하지 않는 방향에서는 “화면을 세로로 돌려주세요” 안내 화면을 표시한다.

예시 판단 기준:

```js
const isPortrait = window.innerHeight > window.innerWidth;
```

#### 검증 방법

가로·세로 회전을 여러 번 반복하고 각 장면의 배경, 버튼, 팝업, 스크롤 영역이 잘리거나 치우치지 않는지 확인한다.

---

### 3.4 브라우저 UI와 안전 영역을 별도로 고려하지 않음

#### 발견된 점

루트 페이지와 iframe이 `100vw`, `100vh`를 사용한다. 각 장면은 고정 캔버스를 viewport에 맞게 축소하지만 브라우저 주소창, 상단 조작 바, 스크롤 바 등의 실사용 영역 차이를 명시적으로 다루지는 않는다.

#### 예상되는 문제

- 전체 화면이 아니면 설계 크기보다 작게 축소되어 글자와 버튼이 작아질 수 있다.
- 주소창이 나타나거나 사라질 때 화면이 다시 확대·축소되며 흔들릴 수 있다.
- 하단 또는 상단에 있는 버튼이 브라우저 UI와 가까워 조작하기 불편할 수 있다.

#### 해결 방법

- 운영 시 브라우저 전체 화면 모드를 사용할 수 있는지 확인한다.
- 가능하면 일반 브라우저 페이지 대신 webOS 패키지 앱 또는 키오스크 실행 환경을 검토한다.
- CSS 동적 viewport 단위 지원 여부를 확인하고 가능하면 `100dvh`를 사용하되 `100vh` fallback을 유지한다.
- 중요한 버튼에 상하 여백을 충분히 확보한다.

#### 검증 방법

주소창이 보이는 상태와 숨겨진 상태 모두에서 화면 전환 및 터치를 확인한다.

---

### 3.5 BGM 및 효과음의 webOS 재생 정책 미검증

#### 발견된 점

루트 페이지는 첫 사용자 상호작용 메시지를 받은 후 BGM을 재생한다. iframe이 바뀌어도 최상위 `<audio>`가 유지되므로 화면 전환 중 BGM을 계속 재생하려는 구조는 적절하다.

```js
bgmPlayer.play().catch(...)
```

다만 BGM은 M4A이고 나머지 효과음은 주로 MP3이며, iframe 내부의 터치가 부모 오디오 재생에 필요한 사용자 제스처로 인정되는지는 webOS 실기기에서 확인되지 않았다.

#### 예상되는 문제

- 첫 터치 후에도 BGM이 재생되지 않을 수 있다.
- BGM과 음성 안내 또는 효과음이 동시에 재생될 때 끊김이 발생할 수 있다.
- 브라우저가 백그라운드 상태가 되거나 화면이 절전된 후 오디오가 복구되지 않을 수 있다.
- M4A 코덱 또는 파일 메타데이터에 따라 로딩 시간이 길어질 수 있다.

#### 해결 방법

- 첫 화면에 명시적인 “시작” 버튼을 두고 그 버튼 이벤트 안에서 BGM을 직접 시작한다.
- BGM에 MP3 또는 AAC 대체 소스를 함께 제공하는 방안을 검토한다.
- `play`, `pause`, `error`, `stalled`, `canplaythrough` 이벤트를 기록할 수 있는 진단 모드를 만든다.
- `pageshow`, `visibilitychange` 이후 필요하면 BGM 상태를 복구한다.

#### 검증 방법

- 브라우저 최초 실행
- 새로고침 후 실행
- 브라우저 홈 이동 후 복귀
- 화면 절전 후 복귀
- BGM과 효과음 동시 재생

위 상황에서 소리가 정상적으로 유지되는지 확인한다.

---

### 3.6 터치 이벤트가 `click`과 중복 실행될 가능성

#### 발견된 점

일부 요소는 같은 핸들러를 `click`과 `touchstart` 양쪽에 연결한다.

```js
btnStart.addEventListener('click', handleStartClick);
btnStart.addEventListener('touchstart', handleStartClick);
```

브라우저에 따라 한 번의 터치로 `touchstart` 이후 합성 `click` 이벤트가 추가 발생할 수 있다.

#### 예상되는 문제

- 한 번 터치했는데 대사가 두 단계 넘어갈 수 있다.
- 애니메이션과 음원이 중복 시작될 수 있다.
- 버튼이 잠깐 나타났다가 바로 다음 상태로 전환될 수 있다.
- 기기나 webOS 버전에 따라 재현 여부가 달라질 수 있다.

#### 해결 방법

- 가능하면 `pointerdown` 또는 `pointerup`으로 입력 체계를 통일한다.
- 터치와 클릭을 모두 유지해야 한다면 시간 기반 중복 방지 또는 상태 잠금을 공통 처리한다.
- `{ once: true }`가 적절한 시작 이벤트에는 일회성 리스너를 사용한다.
- 버튼마다 개별적으로 구현하지 않고 공통 입력 유틸리티를 만든다.

#### 검증 방법

각 버튼을 빠르게 한 번, 두 번, 길게 눌러 화면이 의도한 횟수만큼 전환되는지 확인한다.

---

### 3.7 장시간 실행 시 타이머와 애니메이션 부하 가능성

#### 발견된 점

프로젝트는 다음 기능을 동시에 사용한다.

- `requestAnimationFrame` 기반 눈동자 추적
- CSS 애니메이션 다수
- Canvas 컨페티
- 타이핑 애니메이션
- 퀴즈 2의 50ms 간격 할인 타이머
- 품절 카운트다운
- `MutationObserver`
- 다수 이미지 및 음원 프리로드

#### 예상되는 문제

- PC에서는 원활하지만 webOS 기기에서는 프레임 저하가 나타날 수 있다.
- 퀴즈를 반복하면 정리되지 않은 interval, timeout, animation frame이 누적될 수 있다.
- 장시간 켜두면 터치 반응이 느려지거나 브라우저가 페이지를 다시 로드할 수 있다.
- 컨페티와 다른 애니메이션이 겹칠 때 순간적으로 버벅일 수 있다.

#### 해결 방법

- 화면 종료 시 `setInterval`, `setTimeout`, `requestAnimationFrame`을 명시적으로 정리한다.
- 움직이지 않는 화면에서는 애니메이션을 중지한다.
- `visibilitychange` 시 타이머와 애니메이션을 일시 정지한다.
- 할인 타이머의 갱신 주기를 실제 표시 정밀도에 맞춰 낮추는 것을 검토한다.
- 저사양 모드를 마련해 컨페티 수와 연속 애니메이션 수를 줄인다.

퀴즈 1 컨페티는 2026-08-28 기준으로 퀴즈 2와 동일하게 120개 파티클, 최대 2.5초 실행, `pagehide` 및 화면 이동 시 정리 방식으로 보완했다.

#### 검증 방법

게임 전체를 20회 이상 반복하거나 1~2시간 연속 실행하면서 다음을 관찰한다.

- 터치 응답 시간
- 애니메이션 프레임 저하
- 음원 끊김
- 화면 재로딩 또는 브라우저 종료
- 반복할수록 느려지는 현상

---

### 3.8 화면별 코드 중복과 DOM 구조 결합도가 높음

#### 발견된 점

`도로화면/scene1.html`, `scene2.html`, `scene3.html`에 다음 로직이 상당 부분 중복되어 있다.

- `1080 × 1920` 화면 크기 조절
- 이름 입력
- 커스텀 키보드
- 대사 타이핑
- 음원 재생
- 캐릭터 연출
- 터치 이벤트 처리

또한 `back_button.js`는 각 페이지의 특정 ID와 클래스 상태를 직접 관찰한다.

#### 예상되는 문제

- 한 화면만 수정되어 장면마다 동작이 달라질 수 있다.
- DOM ID를 변경하면 공통 뒤로가기 버튼 표시 규칙이 조용히 깨질 수 있다.
- webOS 호환성 수정이 필요할 때 여러 파일을 반복해서 수정해야 한다.
- 수정 누락과 회귀 오류 가능성이 높다.

#### 해결 방법

- 화면 크기 조절, 입력, 키보드, 오디오, 타이핑을 공통 모듈로 분리한다.
- 뒤로가기 버튼 표시 여부를 DOM 추론 대신 화면이 명시적으로 호출하도록 변경한다.

예시:

```js
window.BackButtonUI?.setVisible(false);
```

- 장면별 HTML에는 콘텐츠와 설정값만 남기는 방향을 검토한다.

#### 검증 방법

공통 모듈화 후 scene 1~3의 이름 입력, 대사 진행, 뒤로가기 버튼 표시가 동일하게 유지되는지 회귀 테스트한다.

---

### 3.9 실제 사용 파일과 과거 작업 파일이 혼재함 — 해결 완료

#### 발견된 점

현재 메인 실행 흐름은 다음 파일을 사용한다.

```text
jeogu_quizgame.html
도로화면/scene1.html
퀴즈1 전반부/index.html
퀴즈1/quiz_game_(4-7).html
도로화면/scene2.html
퀴즈2(0824)/integrated.html
도로화면/scene3.html
```

기존에는 `퀴즈1/screen1.html`부터 `screen7.html`, 개별 JS, `퀴즈2(0824)/screen1.html`, `screen2.html` 등 통합 전 파일도 운영 파일과 같은 디렉터리에 남아 있었다.

2026-08-28 기준으로 통합 전 분리본 19개를 루트의 `archive/`로 이동했다. 실제 운영 HTML과 공유 이미지·음원 에셋은 기존 위치에 유지했다.

#### 예상되는 문제

- 어느 파일이 실제 운영 파일인지 혼동할 수 있다.
- 수정자가 사용되지 않는 파일을 고치고 결과가 반영되지 않았다고 판단할 수 있다.
- 배포 시 불필요한 파일과 에셋이 함께 포함된다.
- 과거 파일에 남은 잘못된 이동 경로가 추후 재사용될 수 있다.

#### 적용한 해결 방법

- 실제 진입점과 전체 화면 흐름을 루트 `README.md`에 명시했다.
- 과거 퀴즈 1·2 분리본을 `archive/` 아래의 별도 디렉터리로 이동했다.
- `archive/README.md`에 보관 목적과 직접 실행을 보장하지 않는다는 점을 명시했다.
- 운영 화면에서 사용하는 통합 파일 및 공유 에셋은 원래 위치에 유지했다.

#### 검증 방법

루트 `README.md`에 기록한 순서대로 전체 게임을 실행하고, 활성 파일이 `archive/` 안의 파일을 요청하지 않는지 확인한다.

---

### 3.10 화면 간 상태 전달 구조가 없음

#### 발견된 점

이름과 퀴즈 진행 상태는 각 페이지 내부 변수로 관리되며 화면 사이에 명시적으로 전달되지 않는다. `localStorage`와 `sessionStorage`는 메인 복귀 시 초기화하지만 주요 진행 상태 저장에는 사용하지 않는다.

#### 예상되는 문제

- 새로고침하면 현재 진행 상태가 사라진다.
- 브라우저가 메모리 문제로 페이지를 다시 로드하면 처음부터 시작할 수 있다.
- scene 1에서 입력한 이름을 이후 장면에서 자동으로 재사용하기 어렵다.
- 중간 화면으로 직접 접속하면 정상적인 선행 상태가 없다.

#### 해결 방법

운영 요구에 따라 다음 중 하나를 선택한다.

- 항상 처음부터 진행하는 체험형 콘텐츠라면 현재 정책을 문서화한다.
- 복구가 필요하면 `sessionStorage`에 최소 상태만 저장한다.
- 저장 항목의 버전을 정의해 코드 업데이트 후 오래된 상태를 안전하게 폐기한다.
- 메인 복귀 시에만 상태를 초기화하고 일반 새로고침에서는 복구하도록 설계할 수 있다.

#### 검증 방법

각 주요 화면에서 새로고침, 홈 이동 후 복귀, 네트워크 단절 후 재접속을 수행해 의도한 시작 지점으로 복구되는지 확인한다.

---

### 3.11 배포 및 실행 방식이 정의되어 있지 않음

#### 발견된 점

현재 프로젝트에는 `package.json`, webOS 앱용 `appinfo.json`, 서버 설정 또는 배포 문서가 없다. 일반 정적 파일 모음이므로 스탠바이미에서 PC의 로컬 파일 경로를 직접 열 수 없다.

#### 예상되는 문제

- 현장 담당자마다 실행 방식이 달라질 수 있다.
- PC 정적 서버의 IP가 바뀌면 접속 주소가 달라진다.
- PC가 절전되거나 방화벽이 요청을 차단하면 콘텐츠가 열리지 않는다.
- HTTPS 페이지와 HTTP 리소스를 혼용하면 브라우저가 일부 요청을 차단할 수 있다.

#### 해결 방법

다음 운영 방식 중 하나를 정하고 문서화한다.

1. 동일 네트워크 PC에서 정적 HTTP 서버 실행
2. 사내 서버 또는 NAS에서 호스팅
3. HTTPS 정적 호스팅 사용
4. webOS 설치형 앱으로 패키징

전시 안정성이 중요하면 외부 PC와 네트워크 의존성이 적은 webOS 패키지 앱을 검토할 가치가 있다. 일반 웹 호스팅을 사용할 경우 고정 URL, 자동 시작, 캐시 정책, 장애 시 복구 절차가 필요하다.

#### 검증 방법

실제 전시 네트워크에서 전원을 완전히 껐다 켠 뒤 담당자가 별도 개발 도구 없이 콘텐츠를 실행할 수 있는지 확인한다.

---

### 3.12 오류가 사용자에게 표시되지 않음

#### 발견된 점

오디오 재생 실패 일부는 콘솔에만 기록하며 이미지, 스크립트, 음원 로딩 실패를 종합적으로 감지하는 화면은 없다.

#### 예상되는 문제

- 현장에서는 빈 화면이나 무반응만 보이고 원인을 알기 어렵다.
- 네트워크 장애와 코드 오류를 구분하기 어렵다.
- 운영 담당자가 개발자 도구 없이 문제를 보고하기 어렵다.

#### 해결 방법

- 시작 시 필수 에셋과 라이브러리를 검사한다.
- 필수 리소스 실패 시 사용자에게 재시도 안내를 표시한다.
- 화면 구석의 숨김 제스처로 진단 정보를 열 수 있게 한다.
- 현재 화면, viewport, 사용자 에이전트, 오디오 상태, 누락 리소스를 표시한다.
- 운영 환경에서는 로그 크기를 제한하고 개인정보를 저장하지 않는다.

#### 검증 방법

의도적으로 음원 또는 스크립트 하나를 차단해 오류 안내와 재시도가 정상적으로 동작하는지 확인한다.

## 4. 권장 우선순위

### P0 — 실기기 투입 전에 반드시 처리

- `hangul-js` 로컬 포함
- 세로 모드에서 실제 viewport 확인
- BGM, 음성, 효과음 재생 확인
- 전체 게임 1회 완주 테스트
- 실제 접근 URL과 서버 운영 방식 확정

### P1 — 안정적인 전시 운영을 위해 권장

- 나머지 CDN 의존성 제거
- 터치·클릭 중복 방지
- 장시간 반복 실행 테스트
- 타이머 및 애니메이션 정리 로직 보강
- 브라우저 UI가 보이는 상태의 화면 대응
- 리소스 로딩 실패 안내 추가

### P2 — 유지보수성 개선

- 도로 장면 공통 코드 모듈화
- 실제 사용 파일과 과거 작업 파일 분리
- 실행 흐름 및 배포 방법을 README로 문서화
- 필요 시 진행 상태 복구 기능 추가

## 5. 스탠바이미 1 실기기 체크리스트

### 실행 환경

- [ ] 화면을 세로로 돌렸을 때 웹페이지도 세로 viewport로 전환된다.
- [ ] `window.innerWidth`와 `window.innerHeight`를 기록했다.
- [ ] 브라우저 UI가 표시된 상태와 숨겨진 상태를 모두 확인했다.
- [ ] 전원을 껐다 켜도 동일한 URL로 다시 접속할 수 있다.

### 화면

- [ ] 모든 화면이 잘리지 않고 중앙에 표시된다.
- [ ] 글자와 버튼 크기가 27인치 터치 환경에서 적절하다.
- [ ] 팝업과 뒤로가기 버튼이 브라우저 UI에 가리지 않는다.
- [ ] 가로·세로 회전 후 화면이 정상적으로 다시 배치된다.

### 입력

- [ ] 한 번 터치했을 때 이벤트가 한 번만 실행된다.
- [ ] 빠른 연속 터치로 장면이 건너뛰어지지 않는다.
- [ ] 한글·영문·숫자 키보드가 모두 동작한다.
- [ ] 쇼핑몰 화면의 세로 스크롤이 부드럽다.
- [ ] 팝업이 열린 동안 배경 요소가 잘못 눌리지 않는다.

### 오디오

- [ ] 첫 사용자 입력 후 BGM이 재생된다.
- [ ] 화면 전환 중 BGM이 끊기지 않는다.
- [ ] 모든 음성 안내와 정답·오답 효과음이 재생된다.
- [ ] BGM과 효과음의 음량 균형이 적절하다.
- [ ] 절전 또는 브라우저 전환 후 오디오가 복구된다.

### 네트워크 및 리소스

- [ ] 브라우저 요청에 404가 없다.
- [ ] 외부 네트워크 없이 핵심 기능이 동작한다.
- [ ] 이미지와 SVG가 모두 정상 표시된다.
- [ ] CDN 장애가 게임 진행을 중단시키지 않는다.

### 장시간 안정성

- [ ] 전체 게임을 20회 이상 반복할 수 있다.
- [ ] 1~2시간 실행 후에도 터치 반응이 느려지지 않는다.
- [ ] 타이머와 애니메이션이 중복 실행되지 않는다.
- [ ] 브라우저가 강제 종료되거나 페이지를 재로드하지 않는다.

## 6. 참고 자료

- [LG 스탠바이미 27ART10AKPL 제품 사양](https://www.lge.co.kr/product/stan-by-me/27art10akpl?modelId=MD08979866&pdpType=PURCHASE)
- [LG StanbyME 세로 모드 문제 해결](https://www.lg.com/us/support/help-library/lg-stanbyme-tv-troubleshooting-vertical-mode--20153192370994)
- [LG webOS Web API 및 Web Engine](https://webostv.developer.lge.com/develop/specifications/web-api-and-web-engine)
- [LG webOS App Resources 가이드](https://webostv.developer.lge.com/develop/getting-started/app-resources)
